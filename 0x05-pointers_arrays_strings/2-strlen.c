/**
* _strlen - a function that returns the length of a string
*@s: a variable used to measure length
* Return: Always 0.
*/
int _strlen(char *s)
{
char *str;
int len;
str = "My first strlen!";
len = _strlen(s);
return (0);
}
