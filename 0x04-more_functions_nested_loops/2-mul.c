#include <stdio.h>
#include "main.h"

/*betty style*/
/**
*mul - function to print multiplication
*@a: the character needed to multiply
*@b: the character needed to multiply
*Return: always 0
*/
int mul(int a, int b)
{
int answer;
a = 5;
b = 9;
answer = a *b;
_putchar(answer);
return (0);
}
