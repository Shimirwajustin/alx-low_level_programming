#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
*main - function
*@argc: integer
*@ argv: array
*Return: 1 or 0
*/
int main(int argc, char *argv[])
{
{
if (argc != 3)
printf("error");
printf("\n");
return 1;
}
int a, b;
a = atoi(argv[1]);
b = atoi(argv[2]);
int c;
c = a + b;
printf("%d", result);
printf("\n");
return 0;
}

