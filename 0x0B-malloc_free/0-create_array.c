#include "main.h"
#include <string.h>
#include <stdio.h>
/**
*create_array - function
*@size: input
*@c: input
*Retirn: not always 0
*/
char *create_array(unsigned int size, char c)
{
if (size == 0)
return (NULL);
else
return create_array;
}
