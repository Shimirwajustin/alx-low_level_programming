#include "main.h"
#include <string.h>
#include <stdio.h>
/**
*create_array - function
*@size: input
*@c: input
*Retirn: not always 0
*/
char *create_array(unsigned int size, char c)
{
int size[10];
if (size == 0)
return (NULL);
else if (size > 0 || c >= 0)
return size;
}
